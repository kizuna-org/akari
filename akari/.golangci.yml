version: "2"
linters:
  default: all
  disable:
    - depguard
    - wrapcheck
    - ireturn
    - testpackage
  exclusions:
    rules:
      - path: (.+)_test\.go
        linters:
          - dupl
          - funlen
      - path: ^go\.mod$
        linters:
          - gomoddirectives

  settings:
    revive:
      rules:
        - name: package-comments
          disabled: true
    varnamelen:
      ignore-decls:
        - i int
